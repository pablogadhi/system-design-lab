# Overrides for infra/charts/app
replicas: 2
port: 3000
probes:
  liveness: /healthz
  readiness: /healthz
route:
  enabled: true
  pathPrefix: /          # catch-all; /api/* routes are more specific and win
  stripPrefix: false
serviceMonitor:
  enabled: false
env:
  HOSTNAME: "0.0.0.0"
