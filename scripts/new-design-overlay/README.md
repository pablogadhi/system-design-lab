# new-design overlay

Files that `scripts/new-design.sh` copies over a fresh design (same paths as in the repo), replacing
`__NAME__` with the design name. Edit them here to change what every new design starts with.
This README itself is not copied.
