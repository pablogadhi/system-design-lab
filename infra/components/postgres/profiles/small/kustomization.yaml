# small: single instance, no replicas. Fast to start; no failover.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
patches:
  - target: { kind: Cluster, name: postgres }
    patch: |-
      - op: replace
        path: /spec/instances
        value: 1
